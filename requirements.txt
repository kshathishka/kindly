# Pinned to the versions the committed .venv was built with, so a fresh
# install reproduces the environment the app was developed against.
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
openai==1.44.1
